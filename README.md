#blank-react-project

## install

```
$ npm install && npm dedupe
```

## run

```bash
$ npm start
```

it runs server at `http://localhost:3001/`

## build

```bash
$ npm run build
```

it will save files into ``.tmp/release/`